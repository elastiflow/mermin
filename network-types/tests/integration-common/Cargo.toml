[package]
name = "integration-common"
version = "0.1.0"
edition = "2024"
license.workspace = true

[features]
default = []

user = ["dep:aya"]

[dependencies]
aya = { workspace = true, optional = true }
network-types = { path = "../.." }
