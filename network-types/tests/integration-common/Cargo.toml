[package]
name = "integration-common"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
network-types = { path = "../.." }
aya = { workspace = true, optional = true }

[features]
default = []

user = ["dep:aya"]
