//! Simple loader example – **not** used by the tests.
//! Run `cargo run` if you want to attach the program manually.

fn main() {
    println!(
        "This crate’s integration tests are run with:\n    ./run_tests.sh\n\
         See README.md for details."
    );
}
