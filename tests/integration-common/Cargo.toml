[package]
name = "integration-common"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
network-types = { path = "../../network-types" }
aya = { workspace = true, optional = true }

[features]
default = []
# Create a "user" feature that enables aya
user = ["dep:aya"]
