[package]
name = "mermin-ebpf"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "mermin"
path = "src/main.rs"

[features]
test = []

[dependencies]

aya-ebpf = { workspace = true }
mermin-common = { path = "../mermin-common" }

[build-dependencies]
which = { workspace = true }
